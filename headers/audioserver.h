#pragma once
#include <string>
#include <list> //Need to look into a better type.


// TCP is the protocol to be used.
class AudioServer
{
public:
    void start(); // Starts the server
    void stop(); // Stops the Server

private:
    ;
}